<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The launch theme only has to survive until Compose draws. Colours come from
         config.json at runtime, so nothing here is generated per build. -->
    <style name="Theme.Web2App" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/splash_background</item>
    </style>

    <style name="Theme.Web2App.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash</item>
        <item name="postSplashScreenTheme">@style/Theme.Web2App</item>
    </style>
</resources>
